{
  "inputs": {
    "feature_dir": ".sdd/specs/001-auth",
    "integration": "codex",
    "feature": "Build the OCP authentication feature with local email/password auth, email OTP verification, Google OAuth login, JWT httpOnly cookie session, bcrypt password hashing, password reset, profile access, role-aware backend authorization, blocked/deleted user rejection, and frontend withCredentials support. Backend remains the source of truth. Do not use Bearer auth, localStorage JWT, frontend role authority, raw OAuth token storage, or user-entered Google credentials."
  }
}